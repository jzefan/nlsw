# 2026-07-26

## 编写 import-to-appdb.js 脚本
- 创建 `scripts/import-to-appdb.js`，用于将 MongoDB 3.6.23 备份数据导入本地 `app_db` 数据库
- 脚本功能：mongorestore 恢复 → 创建 DEFAULT 租户 → 注入 tenantId → 验证
- 验证了本地 MongoDB 用户 `nlsw2026` 对 `app_db` 有写入权限
- 脚本支持 `--dry-run` 和 `--drop` 选项，默认 MERGE 模式
