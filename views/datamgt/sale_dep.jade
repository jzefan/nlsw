//
   Created by ezefjia on 10/27/2014.

extends ../layout
block content
  .page-header
    include tool_group
  .row.margin
    table#data-table.table.table-bordered(style='margin-bottom: 0px !important')
      thead
        tr
          th(style='width:20px;max-width:25px;min-width:10px;') 行
          th 销售部门
      tbody#data-tbody
        if dData
          each sale_dep, index in dData
            tr
              td= index + 1
              td= sale_dep.name
  include data_dialog
  script.
    var local_data = !{JSON.stringify(dData)};
    var local_operObject = !{JSON.stringify(dOperationObject)};
